import { TransformacionPipe } from './transformacion.pipe';

describe('TransformacionPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformacionPipe();
    expect(pipe).toBeTruthy();
  });
});
